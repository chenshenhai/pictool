import { getImageBySrc, getImageDataBySrc } from './util/image-file';
import { compressImage } from './util/compress';

const util = {
  getImageBySrc,
  getImageDataBySrc,
  compressImage,
};

export default util;