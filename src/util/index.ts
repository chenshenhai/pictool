import { getImageBySrc, getImageDataBySrc } from './image-file';
import { compressImage } from './compress';

const util = {
  getImageBySrc,
  getImageDataBySrc,
  compressImage,
};

export default util;